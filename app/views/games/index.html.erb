<div class="outer">
  <div class="inner">
    <div class="pagination-container"><%= will_paginate %></div>
    <ul class="paginated-list">
      <% @games.each do |game| %>
        <li>
          <h3><%= link_to "Game ID: #{game.id}", game %></h3>
          <h4><%= link_to "Delete", game, method: "delete" %></h4>
        </li>
      <% end %>
    </ul>
    <div class="pagination-container"><%= will_paginate %></div>
  </div>
</div>